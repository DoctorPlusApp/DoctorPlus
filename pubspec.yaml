name: doctor_plus_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  tab_indicator_styler: ^1.0.0
  flutter_svg: ^0.19.3

  cupertino_icons: ^1.0.2
  firebase_core: ^2.3.0
  firebase_auth: ^4.1.3
  cloud_firestore: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - "assets/icons/"
    - "assets/fonts/"
    - "assets/logos/"
    - "assets/icons/contentPage/"

  uses-material-design: true